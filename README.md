# Shopping List System

A small CSV-backed Python app for planning meals, tracking inventory, and generating a weekly shopping list.

## Run

```powershell
python app.py
```

The generated weekly shopping list is written to `exports/weekly_shopping_list.csv` and mirrored in `data/shopping_list.csv`.
